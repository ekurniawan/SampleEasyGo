using System;
using System.Collections.Generic;
using System.Text;

namespace SampleXamarinJuly
{
    class SampleRS
    {

        /*private async Task<MyResult> GetAPI(MyParam param)
        {
            var client = new RestClient("https://vtsapi.easygo-gps.co.id");
            var request = new RestRequest("/api/report/idle", Method.POST) { 
                RequestFormat = DataFormat.Json
            };
            request.AddHeader("Token", "");
            request.AddJsonBody(param);
            var response = await client.ExecuteAsync<MyResult>(request);
            return response.Data;
        }*/
    }
}
